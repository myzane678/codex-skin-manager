# 图片主题提示词范例设计

## 目标

在主题库中提供可复制的生图提示词，帮助用户生成不会遮挡 Codex 原生界面的背景图。

## 交互

- 主题库正常状态下，在主题卡片旁显示提示词辅助栏。
- 提供四条 16:9 背景图提示词：留白构图、抽象纹理、自然场景和建筑场景。
- 每条提示词都要求低对比度、低细节安全区、无文字、无徽标、无界面截图。
- 每条提示词的图标按钮写入系统剪贴板，并显示短暂的已复制状态。
- 当主题库宽度不足时，辅助栏移动到主题卡片下方。

## 范围

- 不调用外部生图服务。
- 不改变图片主题的导入、分析、创建和注入流程。
